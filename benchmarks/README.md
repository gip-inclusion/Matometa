# Benchmarks

Performance benchmarks for card retrieval and other tasks.

## Card Retrieval

Tests how well the markdown and SQLite card stores perform at finding relevant Metabase cards.

```bash
python benchmarks/card_retrieval.py
```

Results saved to `card_retrieval_results.json`.
